users:
  ADMIN:
    role: admin
    display_name: Admin
    allowed_states: ["*"]
    password_hash_env: AUTH_ADMIN_PASSWORD_HASH

  QWHITE:
    role: user
    display_name: Quinten
    allowed_states: ["OH", "KY", "TX", "FL", "MI", "NC"]
    password_hash_env: AUTH_QWHITE_PASSWORD_HASH

  JROSS:
    role: user
    display_name: Josiah
    allowed_states: ["IN", "PA", "TN", "AR", "OK", "CO"]
    password_hash_env: AUTH_JROSS_PASSWORD_HASH

  AG:
    role: user
    display_name: Austin
    allowed_states: ["MO", "IL", "GA", "AL", "SC"]
    password_hash_env: AUTH_AG_PASSWORD_HASH

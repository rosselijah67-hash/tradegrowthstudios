# Auth and Territory QA After Fixes

Targeted report-only QA. No SQLite mutation, no external APIs, no Places jobs, no audits, no email sends, and no deployment actions were run.

## 1. P0 Verification

### Railway auth variables

PASS. `docs/RAILWAY_DEPLOYMENT.md` now documents the multi-user auth variables:

- `APP_SECRET_KEY` is generated by `scripts/generate_password_hashes.py --generate-secret` and listed in the Railway variable block at `docs/RAILWAY_DEPLOYMENT.md:28`, `53`, and `64`.
- `AUTH_ADMIN_PASSWORD_HASH`, `AUTH_QWHITE_PASSWORD_HASH`, `AUTH_JROSS_PASSWORD_HASH`, and `AUTH_AG_PASSWORD_HASH` are listed at `docs/RAILWAY_DEPLOYMENT.md:54-57`.
- The doc says Railway should receive generated secret/hash values, not plaintext passwords, at `docs/RAILWAY_DEPLOYMENT.md:31` and `64`.
- Missing user hash env vars are documented as login-blocking for that user at `docs/RAILWAY_DEPLOYMENT.md:33`.

`docs/AUTH_ENV_SETUP.md:8-24` and `.env.example:7-11` also match the new variables. `config/users.yaml:6`, `12`, `18`, and `24` point each configured user at the expected hash env var.

### Obsolete auth variables

PASS. A direct search of `docs/RAILWAY_DEPLOYMENT.md` found no references to the obsolete auth instructions:

- `DASHBOARD_AUTH_ENABLED`
- `DASHBOARD_USERNAME`
- `DASHBOARD_PASSWORD_HASH`
- `FLASK_SECRET_KEY`

The remaining `DASHBOARD_DB_IMPORT_ENABLED` and `DASHBOARD_MEDIA_IMPORT_ENABLED` entries are temporary import toggles, not the old auth system.

## 2. CLI Actor-Scope Verification

Shared actor behavior is centralized in `src/actor_context.py`:

- Actor env is read from `APP_ACTOR_USERNAME`, `APP_ACTOR_ROLE`, `APP_ACTOR_ALLOWED_STATES`, `APP_ACTOR_MARKET`, and `APP_ACTOR_MARKET_STATE` in `src/actor_context.py:42`.
- Actor presence/admin/state helpers exist at `src/actor_context.py:66`, `70`, and `77`.
- Unauthorized market access raises the territory message through `validate_actor_market_access()` at `src/actor_context.py:176`.
- Direct prospect access is resolved and checked by `actor_can_access_prospect()` and `validate_actor_prospect_access()` at `src/actor_context.py:202` and `216`.
- Non-admin SQL scoping is generated by `actor_sql_scope_clause()` and applied by `append_actor_scope()` at `src/actor_context.py:245` and `316`.
- No-actor local/admin mode remains unscoped and logs/prints that no actor territory scope is active at `src/actor_context.py:146` and `245`.

| Module | Actor env detected? | Unauthorized `--market` rejected? | Unauthorized `--prospect-id` rejected? | Non-admin global selection scoped? | Admin/local mode still works? | Real sending gated? |
|---|---|---|---|---|---|---|
| `src/contact_readiness.py` | Yes, via `actor_context` import and shared CLI setup at `src/contact_readiness.py:13`. | Yes, `validate_actor_market_access(args.market, allow_global_scope=True)` at `src/contact_readiness.py:720`. | Yes, `validate_actor_prospect_access(connection, args.prospect_id)` at `src/contact_readiness.py:727`. | Yes, `append_actor_scope()` in `select_prospects()` at `src/contact_readiness.py:394`. | Yes, no actor/admin returns unscoped from `actor_context`. | N/A, no SMTP send path. |
| `src/public_packets.py` | Yes, via `actor_context` import at `src/public_packets.py:13`. | Yes, validation at `src/public_packets.py:633`. | Yes, direct prospect validation at `src/public_packets.py:640`. | Yes, candidate selection appends actor scope at `src/public_packets.py:104`. | Yes, no actor/admin remains local/admin execution. | N/A, no SMTP send path. |
| `src/outreach_drafts.py` | Yes, via `actor_context` import at `src/outreach_drafts.py:11`. | Yes, validation at `src/outreach_drafts.py:1356`. | Yes, direct prospect validation at `src/outreach_drafts.py:1363`. | Yes, candidate selection appends actor scope at `src/outreach_drafts.py:301`. | Yes, no actor/admin remains local/admin execution. | N/A, draft generation only. |
| `src/send_outreach.py` | Yes, via `actor_context` import at `src/send_outreach.py:18`. | Yes, validation at `src/send_outreach.py:821`. | Yes, direct prospect validation at `src/send_outreach.py:828`. | Yes, prospect selection appends actor scope at `src/send_outreach.py:251`. | Yes, no actor/admin remains local/admin execution; real `--send` without actor prints `No actor scope active; treating this as admin/local execution.` at `src/send_outreach.py:834`. | Partially. Territory, approval, draft, suppression, duplicate, daily cap, and compliance gates remain, but see P1 below. |

### `send_outreach.py` send-gate detail

Positive findings:

- Real sends only happen when `--send` is present and `--dry-run` is absent via `send_enabled` at `src/send_outreach.py:812`.
- SMTP/from address/auth, physical address, and unsubscribe requirements are checked in `_validate_send_prerequisites()` at `src/send_outreach.py:746`.
- Candidate selection requires human approval and status/next_action gates for normal batch mode at `src/send_outreach.py:216-251`.
- `_build_candidate()` requires a ready draft, recipient email, non-suppressed email, no duplicate send, subject, and body at `src/send_outreach.py:523-566`.
- Daily cap is enforced before candidate send planning at `src/send_outreach.py:888-902`.
- SMTP send is only called after candidate construction and gates pass at `src/send_outreach.py:949`.

Remaining P1 findings:

- `src/send_outreach.py` has no explicit public-packet requirement; a search found no `public_packet` check in this module. If public packet presence is a mandatory real-send gate for standalone sends, add it before `_send_message()`.
- The direct `--prospect-id --step 1` selection path only requires `human_review_decision = APPROVED` before adding the id filter. It does not enforce the normal `status`/`next_action` gates used by batch selection. This is at `src/send_outreach.py:216-235`.

## 3. Dashboard Route Sanity

No regression found in the inspected dashboard auth/territory surfaces.

- Login protection remains active. Flask-Login is initialized at `src/dashboard_app.py:2053-2063`, and `before_request` redirects unauthenticated non-public endpoints to `/login` at `src/dashboard_app.py:2106-2112`.
- Login/logout routes remain present at `src/dashboard_app.py:2114` and `2143`.
- `APP_SECRET_KEY` is required in production/Railway and only local dev gets a temporary generated secret at `src/dashboard_app.py:1840-1851`.
- Market add still normalizes state and rejects non-owned states with `territory_denial_message()` at `src/dashboard_app.py:1420-1458`.
- Direct case/prospect access still uses `require_prospect_access()` at `src/dashboard_app.py:765`; `/case/<id>` calls it at `src/dashboard_app.py:3038`.
- Queue direct access uses `require_queue_access()` and joins back to `prospects` for territory checks at `src/dashboard_app.py:782-807`.
- Send queue rows are joined to `prospects` and scoped through `apply_prospect_scope()` at `src/dashboard_app.py:5960-6003`.
- Queue creation calls `require_prospect_access()` before inserting queue rows at `src/dashboard_app.py:5593`.
- Quote access uses `require_quote_access()` at `src/dashboard_app.py:774`; quote list scoping applies `apply_prospect_scope()` at `src/dashboard_app.py:4013-4023`; quote detail/edit/status routes call `require_quote_access()` through the inspected route block at `src/dashboard_app.py:2593-2787`.

## 4. Railway Readiness

Can this be deployed to Railway with the new env vars?

Yes, from a code/config perspective, if the deployment branch includes the new auth/territory files and Railway Variables are populated with:

- `APP_SECRET_KEY`
- `AUTH_ADMIN_PASSWORD_HASH`
- `AUTH_QWHITE_PASSWORD_HASH`
- `AUTH_JROSS_PASSWORD_HASH`
- `AUTH_AG_PASSWORD_HASH`

Does the doc mention persistent SQLite volume?

Yes. `docs/RAILWAY_DEPLOYMENT.md:9` states SQLite is persisted on a Railway volume, `docs/RAILWAY_DEPLOYMENT.md:43-52` tells users to mount `/app/storage` and set `USE_STORAGE_SYMLINKS=1` plus `STORAGE_ROOT=/app/storage`, and `docs/RAILWAY_DEPLOYMENT.md:105-119` documents first DB upload to `/app/storage/data/leads.db`.

Any remaining deployment blockers?

No P0 documentation/config blocker remains in the inspected files. Operationally, do not deploy from a branch that omits newly added auth/territory files such as `src/actor_context.py`, because CLI jobs import it.

## 5. Final Verdict

### P0

None found after the fixes in the inspected files.

The previous Railway documentation mismatch is fixed: the runbook now uses `APP_SECRET_KEY` and the configured `AUTH_*_PASSWORD_HASH` variables, and the old single-user auth vars are absent.

### P1

1. `src/send_outreach.py` still lacks an explicit public-packet-exists gate before real SMTP sends.
2. `src/send_outreach.py` direct `--prospect-id --step 1` mode does not enforce the same status/next_action gate as batch mode.

These are not territory-scope regressions, but they should be fixed before allowing real standalone/non-admin send workflows.

### P2

1. Keep `docs/AUTH_TERRITORY_QA_AFTER_FIXES.md` as the current QA artifact, because the older `docs/AUTH_TERRITORY_QA.md` still describes pre-fix findings.
2. Consider adding tiny unit tests around `actor_context.actor_sql_scope_clause()` and `validate_actor_prospect_access()` so future CLI modules cannot accidentally bypass the shared guard.

### Direct answers

Ready for multi-user Railway deployment?
Yes, conditional on committing all new/changed auth territory files and setting the new Railway Variables.

Ready for non-admin outbound workflows?
No for real standalone sending. Territory scoping is in place, but the remaining `send_outreach.py` P1 gates should be closed first. Dashboard-mediated queue/send remains scoped by joined prospect access.

Any remaining no-go conditions?
No P0 no-go condition in the inspected code/docs. The practical no-go conditions are: do not deploy without `src/actor_context.py` included in the deployment branch, and do not enable real standalone non-admin sends until the P1 send gates are fixed.
